import { StyleSheet } from 'react-native'

export const commonStyles = StyleSheet.create({
  input: {
    height: 54,
    width: '70%',
    borderColor: 'tomato',
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
  },
})